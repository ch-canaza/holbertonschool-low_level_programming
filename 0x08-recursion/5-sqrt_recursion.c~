#include "holberton.h"

/**
 * _pow_recursion - function that prints a reverse string
 * @x:power to be printed
 * @y:base
 * Return: char
 */

int _pow_recursion(int x, int y)
{
	if (y < 0)
		return (-1);

	if (y == 0)
		return (1);

	return (x * _pow_recursion(x, (y - 1)));
}
