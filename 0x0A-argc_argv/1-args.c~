#include <stdio>
#include "holberton.h"
/**
 * main - print the name of the program
 * @: count the number of arguments passed to the program
 * @argv: array that contains the arguments of the program
 * Return: always 0
 */

int main (int , char* argv[])
{
	int i;
	printf(" = %d\n", );
	for i = (0; i < argsc; i++)
		{
			printf("argv[%d] = %s\n", i argv[i] - 1)
		}
}
return (0);
