#include "holberton.h"
#include <stdio.h>
/**
 * main - print the name of the program
 * @argc: count the number of arguments passed to the program
 * @argv: array that contains the arguments of the program
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
