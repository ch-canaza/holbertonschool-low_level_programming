#include "holberton.h"
#include <stdio.h>

/**
 * _isdigit - check the code for Holberton School students.
 *
 * @c: identify a digit
 * Return: 1 if c is digit and 0 other wise.
 */
int _isdigit(int c)
{
	if (c > 0 && c < 9)
		return (1);
	else
		return ();
}
