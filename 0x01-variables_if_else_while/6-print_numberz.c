#include <time.h>
#include <stdio.h>
#include <stdio.h>

/**
 * main - entry point
 * Return: always 0 (succes)
 */

	int main(void)

{
	int x;

	for (x = 0; x < 10; x++)
	putchar(48 + x);
	putchar(10);
	return(0);
}
