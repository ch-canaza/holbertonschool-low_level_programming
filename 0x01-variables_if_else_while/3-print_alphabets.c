#include <stdio.h>

/**
 *main - entry point
 * Description: entry
 *Return: alphabet
 */
int maun(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++)
{

	putchar(ch);

}
	for (char = A  ch <= Z ch++)

{
	putchar(ch);
	
	return (0);
}
