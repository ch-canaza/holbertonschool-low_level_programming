#include <time.h>
#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point
 *
 * Return:always 0
 */
int main(void)
{
	int x;
	int y;

	for (x = 48; x < 57; x++)

	for (y = 48; y < 57; y++)

	putchar(48 + x);

	 (x = y)



	putchar(44);
	putchar(32);




	putchar(x);
	putchar(y);
	return (0);
}
